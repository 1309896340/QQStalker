## 1. 专题输入与配置基础

- [ ] 1.1 在时序消息基础上定义讨论专题的结构化数据与独立有效文字过滤器，保留原始序号和同秒顺序、排除系统/撤回/纯媒体/纯表情占位；通过脱敏夹具单元测试验证过滤边界与稳定身份。
- [ ] 1.2 增加 LLM_MAX_DISCUSSION_TOPICS 默认值、正整数校验和 .env.example 说明，并让独立入口与一体化入口在加载环境后、导出或分析前读取它；通过入口 mock 测试验证默认值及无效值在任何导出或 LLM 请求前失败。
- [ ] 1.3 实现带提示词开销预留的专题消息序列化与时序分块，对超长单条消息添加定位与截断标记；通过测试验证每个请求不超过 LLM_MAX_INPUT_CHARACTERS、消息不重排且截断可见。

## 2. 有界议题识别和统计

- [ ] 2.1 定义并解析可验证的分段议题分类响应，校验每条计数消息只有一个主议题、没有未知或重复消息标识；通过格式错误、漏分配和重复分配测试验证修复或可恢复失败路径。
- [ ] 2.2 实现按时间分段的本地议题提炼、预算内递归全局归并和跨段同义议题合并；通过 LLM mock 验证早晚两段的同一议题合并、所有专题调用受预算限制。
- [ ] 2.3 从验证后的归属在本地计算讨论度、首次出现顺序、时间范围、主要参与者与自适应小时/天/周/月桶，并按热度取最多 k 个议题；通过并列热度、议题不足、议题超额和多时段分布测试验证。
- [ ] 2.4 仅为入选议题生成按原始时间推进的多句纪要，并在提示词与输出验证中要求发言者、观点、评价行动及有直接证据时的认同/否认对象；通过无明确回应场景测试验证不虚构立场关系。

## 3. 报告专题与图表渲染

- [ ] 3.1 将专题接入分析主链路和 build_analysis_document，使报告顺序固定为群像速览、讨论纪要、用户画像、语录精选，并在原始 Markdown 提供同源热度降级表示；通过文档组装测试验证专题不进入用户画像卡。
- [ ] 3.2 在 render_html 中从受信任的结构化数据安全注入讨论纪要容器、固定版本 ECharts CDN、堆叠面积序列、稳定图例颜色和失败回退；通过 HTML 断言验证模型文本不能成为脚本、专题 H3 不被成员或语录增强逻辑误包。
- [ ] 3.3 为图表容器添加固定高度、响应式布局和防跨页样式，并让 PNG 渲染器等待图表就绪或失败标记后再测量和截图；通过本地/模拟图表页面验证 PNG 等待、失败降级和分片布局。
- [ ] 3.4 将专题运行期 LLM、归并或 CDN 故障限制在专题区域，保留群像、用户画像和语录精选；通过故障 mock 验证报告继续生成且只显示简短非敏感说明。

## 4. 回归验证

- [ ] 4.1 扩展 tests/test_analyze_transcript.py，覆盖有效消息过滤、配置验证、预算分块、归属完整性、跨段合并、热度排序、时间桶、纪要约束、文档顺序和安全 HTML 组装；运行 uv run python -m unittest tests.test_analyze_transcript 验证。
- [ ] 4.2 扩展 tests/test_generate_portrait.py 及渲染相关测试，验证一体化入口传递专题配置、HTML/PNG 图表就绪和 CDN 失败降级；运行相关 unittest 模块验证。
- [ ] 4.3 使用脱敏的短记录、跨段记录和图表资源失败模拟完成端到端检查，并运行 uv run pyright 和 uv run python -m compileall -q src 验证类型与语法。
