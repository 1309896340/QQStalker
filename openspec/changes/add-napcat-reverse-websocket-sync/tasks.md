## 1. 多包结构与兼容迁移

- [ ] 1.1 创建 `qqstalker_core`、`qqstalker_cli`、`qqstalker_realtime` 包并调整根包发现配置；验证 `uv sync` 和三包导入成功。
- [ ] 1.2 移动 SQLModel、数据库连接、清洗和通用持久化到 core；运行既有导入 / 导出测试，验证表名、约束和结果不变。
- [ ] 1.3 迁移文件导入、导出、分析、渲染 CLI 到 cli，并为旧 `src.*` 命令添加带弃用提示的包装器；验证新旧 `--help` 和现有 CLI 测试。

## 2. FastAPI 与 NapCat 连接

- [ ] 2.1 定义 NapCat 地址、token、群白名单、重连与本机 API 配置；验证 token 不会出现在错误或日志中。
- [ ] 2.2 实现 FastAPI app factory、lifespan、数据库预检和回环 Uvicorn 入口；验证数据库不可用时服务不就绪。
- [ ] 2.3 实现正向 WS 客户端首次连接门槛；用模拟服务验证不可达时以不含 token 的目标参数退出，成功时进入已连接。
- [ ] 2.4 实现 `/healthz`、`/readyz`、`/sync/status`；验证存活、连接就绪和重连未就绪的差异及响应不含敏感内容。

## 3. 事件同步与撤回

- [ ] 3.1 新增 OneBot 数组消息、普通群消息和群撤回 notice 模型；用脱敏 fixture 验证文本、@、群名片、资源元数据和畸形事件。
- [ ] 3.2 实现白名单和事件分派；验证空白名单、私聊、非白名单群和无关事件均不写库且更新忽略计数。
- [ ] 3.3 用 core 的事务 API 保存实时群消息，按群号复用 / 创建会话、保存成员、消息、元素、提及和资源元数据；验证不下载远程附件。
- [ ] 3.4 实现重复事件跳过与唯一冲突恢复；用重放和并发 fixture 验证消息及附表不重复。
- [ ] 3.5 实现撤回 notice：已知目标仅标记 `recalled` 并保留正文，缺失目标直接忽略；用数据库测试验证两种情形。
- [ ] 3.6 实现首次成功后的有界指数退避重连；模拟断开 / 恢复，验证新事件继续同步且不虚假声明补齐缺口。

## 4. 文档与验收

- [ ] 4.1 更新 README 和 `.env.example`，说明三包命令、迁移期、NapCat WS Server、token 保密、白名单、首次失败、重连、状态端点和离线补齐；检查示例无真实凭据。
- [ ] 4.2 添加服务级集成测试，模拟本地 NapCat 发送白名单 / 非白名单消息、撤回、重复消息和失败，验证数据库和非敏感状态 API。
- [ ] 4.3 使用脱敏 OneBot 样本和本机 NapCat 端到端演练，确认实时消息可被迁移后的 CLI 导出；执行 `uv run pyright`、`uv run python -m compileall -q src` 和完整测试集。
