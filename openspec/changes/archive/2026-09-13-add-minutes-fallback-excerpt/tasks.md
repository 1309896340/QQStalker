## 1. 降级摘录实现

- [x] 1.1 新增本地摘录生成函数：按参与者均衡抽取有效消息（每人优先最长一条，轮转补足，上限 8 条）、逐条截断 60 字、按时间排序、`[HH:MM] 成员：内容` 格式；全部消息无效时输出兜底文案。验证：单测覆盖均衡、过滤、截断与空消息兜底
- [x] 1.2 `write_topic_minutes` 失败分支改用摘录：首行标注失败原因（错误消息首句、限长）与摘录性质，不做立场归因。验证：单测模拟纪要请求失败，断言议题分钟含失败原因与摘录内容

## 2. 测试与回归

- [x] 2.1 既有失败占位相关用例更新为新行为。验证：`uv run python -m unittest tests.test_analyze_transcript`
- [x] 2.2 回归验证：`uv run pyright` 与 `uv run python -m compileall -q src` 全部通过
