## 1. 时序消息与配置基础

- [x] 1.1 将导出 Markdown 解析为带序号、时间、发言者、正文和原始文本的时序消息记录，并让成员筛选与活动统计从该记录派生；通过覆盖顺序、成员计数及系统/服务账号排除的单元测试验证。
- [x] 1.2 增加四个上下文环境配置及验证：前后窗口允许单侧为零但拒绝双零，窗口数和每成员字符数必须为正；通过默认值、有效单侧值和无效配置测试验证错误信息。

## 2. 有界上下文构建

- [x] 2.1 实现代表性发言选择：优先显式 `@`、引用/回复、点名或明确提问的有效文字消息，再按时间分层补足；通过测试验证优先级、空内容排除和时间分散。
- [x] 2.2 围绕代表性发言截取前后固定条数的普通成员消息，合并重叠窗口并按序号去重、按时序输出；通过边界消息、跨批次互动对象和重叠窗口测试验证。
- [x] 2.3 为每位成员组装统计、代表性发言和仅出现一次的上下文，并实施每成员字符上限及带标记的单条消息截断；通过测试验证代表性消息不重复、保留发言者/时间和截断标记。
- [x] 2.4 重写批次输入预算分配，预留提示词开销并在 `LLM_MAX_INPUT_CHARACTERS` 内先为每位成员保留基础资料和一个窗口，再轮转加入额外窗口；通过多成员超限测试验证公平保留、总字符硬上限和不可组成最小请求时的清晰错误。

## 3. 提示词与入口接线

- [x] 3.1 更新成员画像提示词和请求序列化，使互动描述仅基于显式指向或语义明确的连续问答，禁止从邻近、同批出现或截断内容推断；通过提示词测试验证允许的跨批次对象、禁止项及最终字段不增加上下文证据。
- [x] 3.2 将有界上下文构建接入批次分析与缺失成员单独补偿，保持成员覆盖、活动统计、群像速览和精选语录流程；通过现有补偿测试和新增请求组装断言验证。
- [x] 3.3 将同一组上下文环境配置贯通独立分析 CLI 与 `generate_portrait.py`，并在 `.env.example` 记录默认值；通过入口参数 mock 测试和配置文件检查验证。

## 4. 整体验证

- [x] 4.1 为含明确 `@`、问答、重叠窗口、上下文不足及超长消息的脱敏最小记录补齐回归测试，并运行 `uv run python -m unittest tests.test_analyze_transcript` 验证。
- [x] 4.2 运行 `uv run pyright` 与 `uv run python -m compileall -q src`，并用脱敏的小型导出做端到端检查，确认请求受限、所有可分析成员均有画像且 HTML 可正常生成。
