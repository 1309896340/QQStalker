## 1. 有效消息清洗修复（discussion_text）

- [x] 1.1 新增裸媒体标记识别：`图片:<文件名>`、`视频:<文件名>` 等裸格式与 `[图片]` 占位同等剥除，纯媒体消息不再进入有效消息集合；新增单测覆盖裸图片、裸视频与文字混排消息，运行 `uv run pytest tests/test_analyze_transcript.py -k discussion` 验证
- [x] 1.2 修复 strip 集合剥坏 `[回复消息]` 开括号的问题（从 strip 集合移除方括号类字符）；新增单测断言清洗后文本保留引用前缀且不出现"回复消息]"残缺形式，运行相关测试验证
- [x] 1.3 核对现有用例与真实导出样本中因 strip 收窄产生的边界差异，补回归用例，确保 `uv run pytest tests/` 全绿

## 2. 句子边界截断辅助

- [x] 2.1 抽出"截到最后一个句末标点"的公共辅助函数，`truncate_minutes` 改为复用；补单测覆盖句末标点在界内/界外、无标点三种情形，运行相关测试验证

## 3. 分层降级（summarize_topic 与摘录兜底）

- [x] 3.1 `summarize_topic` 分段循环改为单段失败丢弃并打 CLI 警告（含完整原因）、以占位跳过；至少一段成功时继续归并；新增单测模拟单段重试穷尽失败，断言议题纪要来自其余分段且不抛错
- [x] 3.2 归并调用失败时拼接当前已通过校验的分段纪要，用 2.1 的辅助函数在句子边界截回 300 字；新增单测模拟归并失败，断言产物为分段纪要拼接且不超过 300 字、无报错文案
- [x] 3.3 全部分段失败时才向上抛错，`write_topic_minutes` 走摘录兜底；`fallback_minutes_excerpt` 去掉 `reason` 参数与失败头部，改为中性引导"主要发言摘录："；新增/更新单测断言正文不含"模型纪要生成失败"、"响应开头"及被拒响应片段
- [x] 3.4 摘录截断改用 2.1 的句子边界辅助（60 字内无标点才以省略号收尾）；更新既有摘录用例断言，验证无半截话输出

## 4. 校验误杀排查

- [x] 4.1 在 LLM 响应缓存中定位纪要校验被拒的原始响应，复现 `_normalize_minutes_text` 拒绝路径并记录结论；若属简单判定缺陷则修复并补测试，若属策略级问题则在 `docs/proposals/` 新增说明文档

## 5. 整体验证

- [x] 5.1 运行 `uv run pyright`、`uv run python -m compileall -q src` 与 `uv run pytest tests/`，全部通过
- [x] 5.2 用真实导出（或缓存重放）复盘一次含失败议题的报告，确认正文零报错、降级产物呈现与正常纪要一致
