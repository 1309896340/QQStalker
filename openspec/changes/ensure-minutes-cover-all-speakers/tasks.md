## 1. 确定性覆盖校验基础

- [x] 1.1 在 `src/qqstalker_cli/discussion_analysis.py` 新增 `find_uncovered_members(text, expected)`：返回未在文本中命中的预期成员名单；命中规则复用姓名高亮链路（完整群名片子串、派生主干别名、ASCII 词边界、冲突别名放弃）。验证：`uv run --with pytest pytest tests/test_analyze_transcript.py -k uncovered`，用例覆盖完整名命中、缩写经别名命中、别名与他名冲突时放弃、英文名嵌在英文单词内不命中
- [x] 1.2 扩展 `MINUTES_COMPRESSION_FEEDBACK`/重写反馈拼装：覆盖缺失时反馈列出缺失姓名，并要求"在 300 字内覆盖全部名单成员、基于其实际发言精简概括、不得为覆盖虚构"。验证：单测断言反馈文本包含缺失姓名与精简概括要求

## 2. 生成侧接入

- [x] 2.1 改写 `_minutes_instruction`（分段与归并两版）：注入该阶段预期主要参与者完整名单，把"不要逐条转述每个人的发言"改为"必须逐一提及每名主要参与者并概括其观点；字数紧张时缩短表述、用概括性转述，而不是省略成员或照抄原句；无实质观点的成员简短中性提及，不得虚构"。验证：单测断言指令含名单与覆盖措辞，且不与句读治理措辞冲突（若 `docs/proposals/minutes-sentence-validation-strategy.md` 对应改动已落地，合并为同一版指令）
- [x] 2.2 `write_topic_minutes` 把展示用 `participants` 传入 `summarize_topic`；`summarize_topic` 为分段阶段与每轮归并计算预期集（该阶段输入中出现过的成员 ∩ 议题前 5 名）并传入 `_bounded_minutes`；`_bounded_minutes` 在 `normalize_minutes` 通过后执行覆盖检查，缺失即按既有重写循环携带定向反馈重写，重写上界维持含首次至多三次 + 1 次兜底。验证：单测用 fake `request_text` 构造"首响应缺人、二次响应补齐"与"始终缺人"两类序列，断言定向重写发生、最终截断兜底仍可达、单请求模型调用次数不超既有上界
- [x] 2.3 降级链与摘录兜底行为不变：覆盖校验失败穷尽后仍走现有句子边界截断，全部分段失败仍走 `fallback_minutes_excerpt`。验证：单测覆盖"覆盖校验穷尽 → 截断结果"与"分段全失败 → 摘录"两条路径不回归

## 3. 覆盖感知的兜底截断

- [x] 3.1 升级 `truncate_minutes`：按句切分后两遍确定性选句——先按原序保留提及尚未覆盖成员的句子，再按原序回填其余句子至 300 字，输出保持句子完整、按原位排列；无预期名单或全部已覆盖时行为与现状一致。验证：单测构造"开头详述 A/B、结尾提及 C"的超长段落，断言截断结果保留 C 的句子且 ≤ 300 字

## 4. 回归与验收

- [x] 4.1 更新既有纪要相关测试中受提示词/反馈文本变化影响的断言，全量跑 `uv run --with pytest pytest tests/`。验证：全部通过（174 passed；另修复归并循环无进展死循环隐患并补拼接降级单测）
- [x] 4.2 静态检查：`uv run pyright` 与 `uv run python -m compileall -q src`。验证：零错误
- [x] 4.3 用既有 LLM 响应缓存回放一个多参与者议题（如 2026-09-12 游戏相关讨论），人工核对最终纪要覆盖全部五名主要参与者、无虚构立场归因；缓存未命中时重新生成的响应经覆盖校验。验证：报告"主要参与者"名单中每人在纪要正文至少被提及一次（离线回放完成：真实缓存响应 + 报告真实 5 人名单，缺失 3 人被判定、反馈列名、209 字覆盖全部 5 人的响应被采纳；未发起真实 LLM 重新生成，该路径由单测 fake 序列覆盖）
